export const clickUpdate = (image) => ({
  type: 'IMAGE_CLICKED',
  payload: {},
})

export const shownUpdate = (image) => ({
  type: 'IMAGE_SHOWN',
  payload: {},
})

export const surveyReset = (image) => ({
  type: 'SURVEY_RESET',
  payload: {},
})
